(function() {
    const app = window.TorCharacterApp = window.TorCharacterApp || {};

    function noop() {}

    app.king = {
        applyKingOfMenBonus: noop,
        removeKingOfMenBonus: noop,
        updateKingOfMenUI: noop,
        init: noop
    };
})();
